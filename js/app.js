// adding an item to a to-do list
document.getElementById("addlist").onclick = addList;
document.getElementById("removelist").onclick = removeList;
document.getElementById("completelist").onclick = completeList;

const addList = function() {
  do {
  let message = window.prompt("Add to List: ");
  if (message) {
  let list = [message];
}
  while (message != null)
}
do {

}
